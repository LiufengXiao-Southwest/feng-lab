当前正在做什么：完成每日文献站改版，首页只展示当天 6 篇，归档按日期查看。

上次停在哪个位置：已完成抓取脚本、首页、归档页、数据迁移和 workflow 修改；验证已通过，但当前环境没有 `.git/objects` 写权限，无法提交和推送。

关键决定：每日抓取结果在全分类中按引用数、影响因子、年份排序取前 6；`papers.json` 只放当天数据，历史全部进入 `archive.json`。
